---
# See options and examples https://docs.ansible.com/projects/lint/configuring/#specifying-configuration-files

profile: basic # min, basic, moderate,safety, shared, production

exclude_paths:
  - .github/
  - devfile.yaml

warn_list:
  - experimental
